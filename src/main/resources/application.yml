eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8091/eureka/

spring:
  redis:
    database: 1
    host: localhost
    port: 6379
    password:
    jedis:
      pool:
        max-active: 8
        max-wait:
        max-idle: 8
        min-idle: 0
    timeout: 10000

ribbon:
  ReadTimeout: 150000